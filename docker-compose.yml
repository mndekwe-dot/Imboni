services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: imboni
      POSTGRES_PASSWORD: imboni
      POSTGRES_DB: imboni
    ports:
      - "5432:5432"
    volumes:
      - imboni_pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  imboni_pgdata:
